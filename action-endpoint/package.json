{
  "name": "dlmm-lp-copilot-action-endpoint",
  "version": "0.2.0",
  "description": "Stateless Solana Action relay for DLMM LP Copilot — previews and echoes plugin-built transactions. Zero runtime dependencies, no secrets, no RPC.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "@types/node": "^26.1.2",
    "bs58": "^5.0.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.65.0"
  }
}
